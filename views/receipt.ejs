<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Receipt - <%= booking.event.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="shortcut icon" href="https://res.cloudinary.com/dehhnip92/image/upload/v1740858902/Occasio_o3cgzl.png" type="image/x-icon">
  <style>
    /* Hide elements with class "no-print" when printing */
    @media print {
      .no-print {
        display: none;
      }
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h2 class="text-center mb-4">Your Booking</h2>
    <div class="card">
      <div class="card-body">
        <h3 class="card-title text-center"><%= booking.event.title %></h3>
        <p><strong>Booking Date and Time :</strong> <%= new Date(booking.bookingDate).toLocaleString('en-US', { 
          dateStyle: 'short', 
          timeStyle: 'short'
        }) %></p>
        <p><strong>Event - Date and time :</strong> <%= new Date(booking.event.date).toLocaleString('en-US', { 
          dateStyle: 'short', 
          timeStyle: 'short',
          timeZone: 'UTC'
        }) %> <b><i>Local Time</i></b></p>
        <p><strong>Location:</strong> <%= booking.event.location %></p>
        <p><strong>Tickets:</strong> <%= booking.numTickets %></p>
        <p><strong>Price per Ticket:</strong> $<%= booking.event.price %></p>
        <p><strong>Total Cost:</strong> $<%= booking.totalCost ? booking.totalCost.toFixed(2) : (booking.numTickets * booking.event.price * 1.13 + booking.numTickets * 2).toFixed(2) %></p>
        <h6>&copy;Occasio - 2025</h6>
      </div>
    </div>
    <div class="mt-3 text-center no-print">
      <button class="btn btn-primary" onclick="window.print()">Print Receipt</button>
      <a href="/bookings" class="btn btn-secondary">Back to Bookings</a>
    </div>
  </div>
</body> 
</html>
