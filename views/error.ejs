<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Occasio - Error</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Occasio Favicon and Custom Styles -->
  <link rel="shortcut icon" href="https://res.cloudinary.com/dehhnip92/image/upload/v1740858902/Occasio_o3cgzl.png" type="image/x-icon">
  <link rel="stylesheet" href="/css/index.css">
  <style>
    body {
      background: #f4f4f4;
    }
    .error-container {
      margin-top: 80px;
      padding: 30px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }
    .error-logo {
      width: 100px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <% if (user) { %>
    <% if (user.role === 'admin') { %>
      <%- include('admin-nav.ejs') %>
    <% } else if (user.role === 'organizer') { %>  <!-- Added condition for organizer -->
      <%- include('organizer-nav.ejs') %>
    <% } else { %>
      <%- include('logged-in-nav.ejs') %>
    <% } %>
  <% } else { %>
    <%- include('navbar.ejs') %>
  <% } %>
  

  <div class="container error-container text-center">
    <!-- Occasio Logo -->
    <img src="../images/Occasio_white.png" alt="Occasio Logo" class="error-logo">
    <h1 class="display-4">Oops!</h1>
    <h2 class="mb-4">Something went wrong</h2>
    <p class="lead">
      <% if (error.status) { %>
        Error <%= error.status %>: <%= error.message %>
      <% } else { %>
        We’re sorry, but something unexpected occurred.
      <% } %>
    </p>
    <a href="/" class="btn btn-primary">Return Home</a>
  </div>

  <!-- Include your footer for consistency -->
  <%- include('footer') %>
  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
